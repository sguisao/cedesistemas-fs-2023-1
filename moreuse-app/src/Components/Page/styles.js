import styled from "styled-components";
import { TopbarHeight } from "../../globalStyles";

export const PageContainer = styled.section`
  margin-top: ${TopbarHeight + 20}px;
`;

export const PageTitleContainer = styled.div`
  margin: 0 20px;
  text-align: center;
`;
